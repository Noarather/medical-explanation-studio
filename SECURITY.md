# 安全与数据边界

- 不要提交真实 API Key、访问令牌、证书、数据库、教材、真实题库、患者信息或含这些内容的日志／截图。
- `config.yaml` 只包含环境变量占位符和非敏感默认值。真实密钥通过系统凭据管理器或环境变量提供。
- 用户数据位于本机应用数据目录。程序会在已配置云功能被调用时向相应服务发送题目、教材片段或 OCR 页面，详见 README；请先确认你有权这样处理数据。
- 公开 Issue / PR 中只提交脱敏的最小复现和合成样本。不要把密钥或私人材料粘贴到公开讨论。
- 如密钥曾泄漏，应先在提供商侧撤销／轮换，再处理文件、Git 历史及缓存；添加 `.gitignore` 不能清除历史中的秘密。

提交前运行 `python tools/check_public_source.py`。这是基础静态防护，不是“没有敏感信息”的绝对保证；新增数据文件、依赖和发布包仍须人工审核。
