# 批量录题与异常审核

本轮更新将审核改为默认自动校验：生成任务结束、打开审核工作台、导出前都会检查已生成且待审核的题目。通过的题目进入“已批准”，无需逐题打开。

## 使用流程

2.0.1 起，“检查批次”、恢复校验和提交在后台运行，显示阶段进度；读取／校验可取消，进入原子入库提交后须等待完成。切换页面不会终止任务，回到导入页可重新接上进度；关闭程序前须完成或取消批量任务。批量异常每页 50 条，可以直接翻到尾页；默认使用字段表单修改题干、题库、学科、选项与答案，也可切换高级 JSON 编辑。未编辑的病例及扩展字段保留。

1. 在“题目导入 → 批量导入”选择多个 JSON / Excel 文件。设置一次缺省题库、学科、章节和来源；源文件已有值优先。可把这些设置及普通 Excel 字段映射保存为来源模板。
2. 点击“检查批次”。合格题显示数量；完全重复的题自动跳过；只有异常题展开编辑。批次自动保存，可从“恢复已保存批次”继续。
3. 点击“导入全部可用题目并开始生成”。默认勾选自动生成，使用现有教材和模型配置。可取消勾选，只入库。校内、考研会分别建立题目集。
4. 生成结束后自动审核。审核工作台默认显示待复核题，每题有具体原因。可以关闭自动审核，恢复人工模式。
5. 需要回写网页已有题目时，使用“导出中心 → 自动检查并导出变更题”。导出文件只包含已通过且有变化的题目；异常另外写入检查报告。上传到网页现有的管理员 v2 导入入口。

普通文本整理会保留本地识别成功的题，仅将失败题块交给 AI。AI 失败时成功题仍保留；原文未找到答案标记或有共享前文时，需核对来源。完成核对后可选中这些题，点击“已核对选中题原文，重新校验”；字段错误仍会继续拦截。整理前请选择缺省题库。

填空题使用 `type: "fill"`、`options: []`，题干为连续的 `【1】`、`【2】`，答案为二维数组，例如 `[["ATP", "三磷酸腺苷"], ["乙"]]`。预览、JSON、Excel 均保留候选答案和大小写。

## 自动通过条件

当前策略 v1 要求：教材解析、无生成错误、匹配分数至少 0.75、A 级证据、教材名及有效印刷页码和证据文本齐全；题干、答案、选项合法；解析包含考点、答案依据、易错点表格；有简析及标签。缺证据、通识解析、结构错误或人工标记的题继续人工复核。

这些条件校验结构与证据完整性，并不等于独立验证医学结论。实际自动通过比例取决于题目和教材质量，不预设通过率。每次自动批准写入 `review_actions`，逐题判断写入 `automatic_review`，包含策略版本、内容指纹、原因和时间。人工驳回不会被自动批准；同一内容人工退回后也不会再次自动批准。

## 去重与增量更新

- 重复导入会再次检查数据库，完全重复跳过。相同 ID 或相同题干、病例、选项但答案／其他内容有差异时保留为异常。同一批次冲突的两侧都暂停导入，不默认采用第一条。尚未提供语义近似题自动合并。
- `question_baselines` 只在新题导入时记录原始快照。旧题无快照时不能安全生成增量文件，检查报告会提示；可从网页重新导出并以新题目集导入后处理。追加新题不会伪造旧题快照。
- 增量文件中的题目沿用 v2 字段，在 `extensions.medqPatch` 中声明更新字段和对应原值。外层使用 `medlearning.question-patch` v1 标记，旧服务器会拒绝该文件，避免被当作完整替换。网页端只合并声明字段，保留未涉及内容；题干、病例、答案、选项、所属题库等发生变化会报冲突。
- 网页预览和发布时都会核对原值。已应用文件可重复上传，已有第三方修改则返回冲突。文件内未声明的空字段不会清空网页内容；显式声明的清空操作会生效。
- 本轮未加入网页自动发布。网页服务器需要更新本轮的 `server.js` 和 `lib/question-patch.js` 后才能接收增量文件；旧服务器应继续使用完整导出。

## 验证与运行

离线测试覆盖：自动通过与异常、人工退回、关闭策略、答案合法性、填空 JSON/Excel 往返、模板、重复提交、批内冲突、异常恢复、局部 AI 失败、快照保护、Python 到网页的增量协议。测试使用临时数据库和模拟模型，不会调用真实模型服务。

源码运行仍使用现有 `.venv` 启动 `desktop_app.py`，前端使用 `frontend/dist`。已有安装版 EXE 需要重新打包才能包含本轮代码。
