# 参与开发

1. 使用 Python 3.13 和受支持的 Node.js / npm，按 README 安装依赖。
2. 测试只使用临时数据库、合成题目和模拟模型服务，不提交真实题库或教材。
3. 修改后运行 Python 测试、前端测试、类型检查及 `python tools/check_public_source.py`。
4. PR 说明行为变化、验证结果和限制。对医疗内容、检索准确率和模型忠实度不要做未经评测的承诺。
5. 贡献者应有权提交对应代码／材料，并同意其按项目 AGPL-3.0-only 许可证分发。

源码仓库不包含私用网页项目。依赖相邻网页项目的跨项目协议测试在缺少该项目时会明确跳过，其余本地协议测试仍执行。

构建安装包需要安装运行依赖，但 2.2.0 起不再下载本地 AI 模型；只需生成分发产物时使用 `build_windows.ps1 -SkipLocalInstall`，避免意外覆盖自己的已安装程序。上传二进制前另行检查第三方许可和源码提供义务。

维护者每次更新应同步 CHANGELOG.md；经测试、安装验收及敏感内容审查后再推送源码。不要把本机业务数据、日志或内部交接记录带入提交。Release／安装包应单独审查发布。
