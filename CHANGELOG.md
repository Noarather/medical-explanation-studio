# 更新日志

## 2.2.2 — 2026-09-26

- 题目管理新增“整批首次生成解析”：选择导入批次后预览全批次数量，跨分页处理，不受搜索、筛选或勾选范围影响。
- 仅提交等待中的无解析题目，保留已有解析及审核结果；已处理／失败题目仍通过审核工作台重试。
- 提交前确认云端费用并选择索引可用的教材；服务端重新校验候选快照，同批次活动任务阻止重复提交。使用原有生成队列和任务中心查看进度。

## 2.2.1 — 2026-09-25

验证记录：公开源码离线测试 376 项、前端 105 项通过，类型检查通过；Windows 构建及本机安装的解析、复核入口、费用确认和原生滚动验收通过。真实模型测试仅使用单张合成双栏页，不代表医学教材准确率评测。

- 教材库新增“高级 OCR 复核”：指定一个 PDF 物理页，并排比较原图、当前 OCR 与 qwen3.8-max / qwen3.7-plus 的转写。
- 点击前明确同意云端与费用说明；每次最多调用两个模型各一次，不自动重试、不自动切换整本教材的默认模型。
- 本机保存原图、原始输出、整理后文本、请求 ID、耗时及已返回的 token 用量，可免费查看最近 20 条记录。失败保留另一模型的结果；不覆盖教材索引或历史证据。
- 通用 Qwen3 视觉模型关闭思考，避免 OCR 场景长时间等待；专用 OCR 参数保持不变。
- 合成单页验证三个模型的双栏顺序与指定数值均通过；高级模型关闭思考后调用成功。样本不足以宣称旗舰模型全书识别更准确，暂不启用自动疑难页替换。

## 2.2.0 — 2026-09-25

验证记录：公开源码离线测试 365 项通过，前端 103 项通过并完成类型检查；Windows 构建及本机安装的解析、界面、原生滚动和索引状态刷新验收通过。真实云 OCR 仅做合成小样本验证，不代表整本教材准确率评测。

- 教材解析改为轻量文字层提取＋Qwen 云端 OCR，默认 `qwen3.5-ocr`；移除安装包中的 Docling、RapidOCR、本地推理运行时和模型权重。
- 扫描页、双栏检测与强制 OCR 直接走云端；加入超时、受控重试、空响应／截断拒绝和请求 trace。适配 Qwen 的文字 LaTeX 外壳、数字页码和 TSV 表格输出。
- 名称／学科／版本识别、扫描件页码校准改用云 OCR，并显示数据发送和费用提示；不确定的印刷页仍需人工校准。
- 保留逐页断点、向量断点、事务性发布和失败重试。保留兼容向量空间的旧索引，不因更换解析器自动重建。
- 升级前一致性备份；仅清理安装目录中的旧本地 AI 资源，不删除教材、题库、凭据及旧版发布包。
- 构建与安装探针切换为文字层＋模拟云 OCR，明确区分离线测试和真实模型小样本验证。
- 后续更新默认维护本日志，在本机安装验收和敏感内容审查后同步 GitHub 源码；不自动发布未经审查的安装包或业务数据。

## 2.1.3 — 2026-09-25

- 教材索引改为磁盘暂存、解析／向量化断点续跑和原子发布。
- 本地解析内存压力下缩小批次，工作进程支持有进展的受控重启。
- 保留批量教材导入、页码校准、中文路径兼容和索引状态刷新修复。
